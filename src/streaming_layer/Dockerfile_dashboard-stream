FROM python:3.10

EXPOSE 8502

WORKDIR /home/user/src/streaming_layer

COPY . .

RUN apt-get update &&\
    pip install -r requirements.txt

ENTRYPOINT ["streamlit", "run", "dashboard-stream.py", "--server.port=8502", "--server.address=0.0.0.0"]
