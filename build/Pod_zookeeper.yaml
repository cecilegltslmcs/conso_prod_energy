apiVersion: v1
kind: Pod
metadata:
  name: zookeeper
spec:
  containers:
  - image: 'bitnami/zookeeper:latest'
    name: zookeeper
    imagePullPolicy: IfNotPresent
    env:
    - name: ALLOW_ANONYMOUS_LOGIN
      value: yes
    ports:
      - containerPort: 2181